# Local development (npm run dev) — used by .env
VITE_API_BASE_URL=http://localhost:4000
VITE_APP_NAME=CDF Formulation
VITE_REPORT_BRAND_NAME=CND

# Production (Vercel / npm run build) — also set in Vercel → Settings → Environment Variables:
# VITE_API_BASE_URL=https://cdf-backend-t961.onrender.com
